{
  "arcPay": [
    "InvoicePaid(bytes32 indexed invoiceId,address indexed payer,address indexed merchant,uint256 grossAmount,uint256 merchantAmount,uint256 protocolFee,bytes32 memoHash)",
    "PaymentRefunded(bytes32 indexed invoiceId,address indexed merchant,address indexed payer,uint256 amount)",
    "ProtocolFeesWithdrawn(address indexed treasury,uint256 amount)"
  ],
  "agentPay": [
    "Funded(address indexed from,uint256 amount)",
    "Withdrawn(address indexed to,uint256 amount)",
    "AgentPolicySet(address indexed agent,uint256 perPayment,uint256 dailyLimit,uint64 validUntil,bool enabled)",
    "MerchantPermission(address indexed agent,address indexed merchant,bool allowed)",
    "AgentInvoicePaid(address indexed agent,bytes32 indexed invoiceId,address indexed merchant,uint256 amount,uint256 spentToday)"
  ],
  "agentPayFactory": ["VaultCreated(address indexed owner,address indexed vault,uint256 initialFunding)"],
  "agentPassport": [
    "WalletBound(uint256 indexed agentId,address indexed oldWallet,address indexed newWallet,address owner)",
    "WalletUnbound(uint256 indexed agentId,address indexed oldWallet,address owner)"
  ],
  "agentPayV3": [
    "Funded(address indexed from,uint256 amount)",
    "Withdrawn(address indexed to,uint256 amount)",
    "AgentPolicySet(uint256 indexed agentId,uint256 perPayment,uint256 dailyLimit,uint64 validUntil,bool enabled)",
    "MerchantPermission(uint256 indexed agentId,address indexed merchant,bool allowed)",
    "AgentInvoicePaid(uint256 indexed agentId,address indexed agentWallet,bytes32 indexed invoiceId,address merchant,uint256 amount,uint256 spentToday)"
  ],
  "agentPayFactoryV3": ["VaultCreated(address indexed owner,address indexed vault,uint256 initialFunding)"],
  "agentJobs": [
    "JobCreated(uint256 indexed jobId,address indexed client,address indexed provider,address evaluator,uint256 budget,uint64 expiry,bytes32 descHash,uint256 providerAgentId)",
    "JobSubmitted(uint256 indexed jobId,bytes32 deliverableHash)",
    "JobCompleted(uint256 indexed jobId,address indexed provider,uint256 budget,uint256 providerAgentId,bytes32 evidenceHash)",
    "JobRejected(uint256 indexed jobId,address indexed client,uint256 budget,bytes32 evidenceHash)",
    "JobExpired(uint256 indexed jobId,address indexed client,uint256 budget)"
  ],
  "reputationRegistry": [
    "read: getClients(uint256 agentId) returns(address[])",
    "read: getLastIndex(uint256 agentId,address client) returns(uint64)",
    "read: readFeedback(uint256 agentId,address client,uint64 index) returns(int128 score,uint8 decimals,string tag1,string tag2,bool isRevoked)",
    "write: giveFeedback(uint256 agentId,int128 score,uint8 decimals,string tag1,string tag2,string endpoint,string fileuri,bytes32 filehash)"
  ],
  "validationRegistry": [
    "read: getAgentValidations(uint256 agentId) returns(bytes32[])",
    "read: getValidationStatus(bytes32 dataHash) returns(address validator,uint256 agentId,uint8 response,uint256 lastUpdate)",
    "write: validationRequest(address validatorAddress,uint256 agentId,string requestUri,bytes32 dataHash) — agent-owner only",
    "write: validationResponse(bytes32 dataHash,uint8 response,string responseUri,bytes32 responseHash,string tag) — requested validator only"
  ],
  "arcLend": [
    "Supplied(address indexed user,uint256 assets,uint256 shares)",
    "Borrowed(address indexed user,uint256 amount)",
    "Repaid(address indexed user,uint256 amount)",
    "Liquidated(address indexed user,address indexed liquidator,uint256 repaid,uint256 collateralSeized)",
    "OracleSynced(uint256 price,uint64 updatedAt)",
    "BadDebtRecorded(address indexed user,uint256 amount)"
  ]
}
