# dependencies & build output
node_modules/
dist/
*.tsbuildinfo

# secrets / local env (public build config .env.production + .env.example are kept)
.env
.env.local
.env.*.local

# foundry artifacts (out/ is regenerated; cache/ can hold sensitive run data)
contracts/out/
contracts/out-v3/
contracts/cache/

# misc
.DS_Store
*.log
public/developers/agents.json.state.json
public/developers/jobs.json.state.json
public/developers/services.json.state.json

# third-party agent-framework noise dropped into the tree by local tooling —
# never part of this project, never meant to ship
.claude-flow/

# scratch / generated — not source
tmp/
test-results/
exports/
